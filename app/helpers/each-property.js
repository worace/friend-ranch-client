import Ember from 'ember';

export default Ember.Handlebars.helper('each-property', function(context, options) {
  //var ret = "";
  //for(var prop in context)
  //{
      //ret = ret + options.fn({property:prop,value:context[prop]});
  //}
  return "What";
  //return ret;
});
